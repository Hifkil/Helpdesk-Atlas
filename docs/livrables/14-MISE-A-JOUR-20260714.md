# Mise à jour du pack — 14 juillet 2026

## Sessions des 13 & 14 juillet — état consolidé

### rp01 + TLS — ✅ RECETTÉ, PHASE 4 CLOSE (14/07)
- **Décision d'équipe tracée** : CA interne (« trust interne documenté » = texte du sujet) plutôt que Let's Encrypt.
- CA « Atlas Internal Root CA » : racine EC P-256 5 ans (clé au coffre, hors Git — vérifié), cert `helpdesk.atlas.local` SAN 1 an, script `make-ca.sh` versionné dans `ca/`.
- rp01 (premier clone v4) : Nginx TLS 1.2+/HTTP2, redirect 80→443, HSTS, rate limiting, proxy→glpi01.
- **Preuve de chaîne** : `curl --cacert` **sans -k** → HTTP/2 200, depuis rp01 ET depuis un poste client. Cadenas « PME Atlas » au navigateur avec la racine importée.
- URL de base GLPI passée en https. Reboot de validation : **43 s**, chaîne auto-relancée.
- **Règle floating « deny direct glpi01:80 »** (contournement TLS bloqué) — contre-tests exit 28 depuis 2 zones, chaîne légitime intacte. Leçon structurante : les règles s'évaluent sur l'**interface d'entrée** ; l'intra-VLAN ne traverse pas fw01.

### VPN WireGuard admin — ✅ EN SERVICE (14/07)
- Instance wg-admin sur fw01 : UDP 51820 (seule règle entrante WAN), subnet 10.60.0.0/24, peer harry-pc avec **PSK**, split tunnel.
- **Split-DNS** : contournement de la collision `.local`/mDNS de systemd-resolved via routing domain `~atlas.local` (persisté en PostUp). Leçon itération 2 : préférer `.internal`.
- Remplace les tunnels SSH ; ProxyJump conservé en **accès de secours documenté**. L'endpoint = Additional IP → le VPN **suit l'IP move** en bascule DR (argument R4).
- Debug tracé : peer non-Enabled (WireGuard ignore silencieusement les inconnus), tiret cadratin dans la config détecté par `cat -A`.

### Template v5 — ✅ cloud-init (14/07)
- v4 + cloud-init (NoCloud/ConfigDrive), `--ciuser root --sshkeys`, lecteur ide2 cloudinit.
- `regen-ssh-keys.service` retiré (cloud-init régénère les clés lui-même).
- **Rituel R0 sans console** : `qm clone` + `qm set --ipconfig0 ip=…,gw=… --nameserver --searchdomain` + `qm start`. Hostname dérivé du --name.
- Incident mineur documenté : première consécration sans la préparation intérieure (détecté, refait) ; protection héritée frappe une 2e fois (998).
- ⚠️ Clones v5 : interface `eth0` (plus `ens18`).

### mail01 — 🔶 PROVISIONNÉ (14/07) — REPRENDRE ICI
- VM 230, 10.20.0.20, DMZ — **premier provisionnement cloud-init, recette v5 réussie** : SSH par clé dès le premier boot, ~40 min chrono pour v5 + mail01.
- **Prochaine étape** : Postfix relais interne (Internet Site, mynetworks restreint à glpi01, boîte demo via luser_relay), test SMTP local + depuis glpi01 (session brute /dev/tcp), config GLPI Notifications, **test critique ticket→mail**, reboot de validation.
- Rappel architecture : glpi01→mail01:25 = intra-VLAN, aucune règle fw01 requise ; règle à créer plus tard pour zbx01→mail01:25.

### Décision tracée
- **Ansible écarté** pour cette itération (arbitrage temps vs preuves PRA) — runbooks rejoués + futur `scripts/recette.sh` à la place. Réponse soutenance préparée.

## Fichiers mis à jour dans ce pack
- `00-ETAT-PROJET.md` (décisions TLS/VPN, statuts rp01/glpi01/mail01, template v5, reste à faire, +5 leçons)
- `11-journal-de-preuves.md` (+13 lignes 13-14/07)
- `14-MISE-A-JOUR-20260714.md` (ce fichier)
- `README.md` du repo (phase 4 ✅, VPN, v5) — voir version jointe

## Chiffres nouveaux (mesurés)
- Reboot rp01 → chaîne TLS complète : **43 s**
- v5 + provisionnement mail01 : **~40 min** (vs ~10 min de rituel manuel PAR VM auparavant)
- Contre-tests floating : exit 28 ×2 zones, 200 via rp01

## Reste ouvert (rappel court)
1. **mail01/Postfix** (reprendre ici) → zbx01
2. pvesr 15 min + dumps SQL → pbs01
3. Matrice de flux (retrait TEMP) · durcissement sshd v3 · GUI OPNsense MGMT only
4. Incidents 1 & 2 + R4/R5 · budget OVH · gel J-4

## Munitions soutenance (nouvelles)
- Démo d'ouverture en boîte : *« VPN admin → https://helpdesk.atlas.local → cadenas PME Atlas »* — 5 exigences en une phrase.
- La règle floating + les 2 contre-tests = « défense en profondeur » avec preuve.
- Trajectoire template v1→v5 : une histoire d'amélioration continue à raconter (bugs regen-ssh-keys → protection → dhcpcd → cloud-init).
