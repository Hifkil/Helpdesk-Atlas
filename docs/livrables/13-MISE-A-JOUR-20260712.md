# Mise à jour du pack — 12 juillet 2026

## Séance des 10 & 12 juillet — état consolidé

### TRAVAUX-02 — ✅ CLOS (10/07)
- Test 5 : exposition 22/443 filtrée depuis l'extérieur (3 méthodes).
- Test 6 : reboot de validation fw01 à 21:11:15 UTC, **coupure ≈ 45 s** (ping horodaté), contre-recette 1-4 verte.
- Nettoyage : VM d'essai 90909 + 1001 détruites (elles seules référençaient encore vmbr2), bloc `vmbr2` + MASQUERADE + `ip_forward` retirés de hv01. **Chemin unique via fw01.**

### glpi01 — ✅ RECETTÉ (10/07)
- Découverte : GLPI **11.0.8** était déjà complètement installé depuis le 05/07 21:37 (442 tables) — la doc avait pris du retard sur l'exécution.
- Durcissement : 4 comptes par défaut désactivés (preuve SQL), `install.php` supprimé, superadmin nominatif.
- Timer `glpi-cron` (1 min, persistent). Reboot de validation : HTTP 200 au retour.
- Réserve documentée : HTTP interne only, TLS via rp01.

### Template tpl-debian13 — ✅ v4 (12/07), après incident
- **Incident documenté** : le v3 (9000) avait été détruit par accident le 05/07 lors d'un sweep de nettoyage (UPID `6A4A8649`). Impact prod nul (full clones).
- v4 = recette v3 + trois correctifs : purge **dhcpcd-base** (installé d'office par l'installeur Debian 13 → APIPA + resolv.conf écrasé sur les clones) ; **regen-ssh-keys sans ExecStartPost** (deadlock systemd corrigé) ; durcissement sshd **réellement appliqué et testé** (password refusé).
- Anti-récidive : `--protection 1` + tags `TEMPLATE,ne-pas-supprimer`.

### Écart réseau — ✅ détecté & corrigé (12/07)
- DHCP **Dnsmasq** actif sur MGMT côté OPNsense (plage 10.90.0.41–245 héritée du LAN initial) — découvert via un bail parasite pris par la VM template.
- Service désactivé entièrement (il ne servait que ce DHCP ; DNS sur port 53053, non utilisé). Contre-test : `busybox udhcpc` → **No DHCPOFFERS**. Conforme « tout statique ».

### pbs01 — ✅ EN SERVICE (12/07, sur hv02)
- ISO officielle PBS 4.x ; datastore `atlas-backups` sur disque dédié 100 Go ; compte moindre privilège `pve-backup@pbs` (DatastoreBackup) ; fingerprint pinné au raccordement.
- **Placement hv02 = décision d'architecture** : séparation du domaine de panne (prod sur hv01, sauvegardes en face). pbs01 non sauvegardé (récursivité) : reconstructible ~20 min, datastore protégé par ZFS mirror hv02.
- Premier backup : 4 VM en **101 s** (~13 GiB utiles). Verify : **4/4, 0 erreur**. Job quotidien 21:00 en mode **Exclude** (120+9000), validé : re-passage en **4 s** (dirty-bitmaps). Prune 7j/4sem + GC quotidiens.

### Test R2 — ✅ première mesure RTO réelle (12/07)
- glpi01 restauré vers VM 999 : **restore 18 s**, services ~2 min, **bout-en-bout 8 min 21 s** (majoritairement des manips de test : isolation link_down, IP décalée).
- Refus `glpi_app`@`.99` par MariaDB = preuve moindre privilège. RTO réel estimé **2-3 min** (IP identique en incident). Exigence ≤ 40 min : **marge ×5 à ×15**.
- Détail complet : journal de preuves §2 bis.

## Fichiers mis à jour dans ce pack
- `00-ETAT-PROJET.md` (statuts, template v4, sections glpi01/pbs01/test R2, reste à faire, leçons)
- `11-journal-de-preuves.md` (13 nouvelles lignes horodatées + §2 bis tableau RTO + §5 sauvegardes)
- `TRAVAUX-02-wan-additional-ip.md` (recette 6/6, clôture)
- `13-MISE-A-JOUR-20260712.md` (ce fichier)

## Reste ouvert (rappel court)
1. rp01 (décision TLS d'équipe à trancher d'abord) → mail01 → zbx01
2. Réplication pvesr 15 min (6 VM) + dumps SQL db01 → pbs01
3. Matrice de flux (retrait des 2 règles TEMP)
4. Durcissement sshd sur dns01/glpi01/db01 (héritage v3)
5. Incidents 1 & 2 en conditions de démo + R4/R5 (IP move)
6. Budget 04 : montant mensuel Additional IP (facture OVH)

## Points volontairement non inventés
- Montant mensuel exact de l'Additional IP : toujours à recopier depuis la facture OVH.
- Heures approximatives marquées « ~ » dans le journal : à préciser via Task History Proxmox/logs OPNsense si une preuve plus fine est requise ; ne pas inventer.
- Les ⬜hh:mm des 04-05/07 restent à retrouver (history / horodatage captures / tasks).

## Munitions pour la soutenance (nouvelles)
- **Chiffres** : restore 18 s · backup incrémental 4 s · coupure reboot FW 45 s · RTO test 8 min 21 s vs 40 min exigées.
- **Anecdotes détection→correction→contre-recette** : template détruit → protection 1 ; DHCP fantôme → dnsmasq off + No DHCPOFFERS ; linked clone bloquant → règle full-clone vécue ; ACL DB refusant la VM de test → moindre privilège prouvé « par accident ».
- **Décision d'archi défendable devant jury non technique** : « les sauvegardes vivent sur l'autre serveur que la production, et le serveur de sauvegarde ne se sauvegarde pas lui-même ».
